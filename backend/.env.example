# Provedor de IA: "openai" (padrão) ou "gemini"
LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Gemini (usado quando LLM_PROVIDER=gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Timeout (segundos) da chamada à LLM
LLM_TIMEOUT_SECONDS=60
